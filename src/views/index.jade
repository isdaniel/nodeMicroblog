extends layout

block content
  - if (!user)
      .jumbotron.hero-unit
        h1 Welcome to Microblog
        p Microblog is powered on Node.js
        p
          a(class='btn btn-primary btn-large', href='/login') Login
          a(class='btn btn-large', href='/reg') Register
  - else
      .container
        .row
          include say
          include posts
            
